[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page site, but this keeps deep links working if a router is added later.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
